[package]
name = "omnipanel-engine-postgres"
version.workspace = true
edition.workspace = true
publish = false

[[bin]]
name = "omnipanel-engine-postgres"
path = "src/main.rs"

[dependencies]
omnipanel-db = { path = "../omnipanel-db", default-features = false, features = ["engine-postgres"] }
tokio = { workspace = true }
