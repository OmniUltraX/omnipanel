# DB / i18n 激进结构优化 Implementation Plan

> **For agentic workers:** 按任务顺序；完成后 `cd frontend && npx tsc -b`。

**Goal:** Panel/Grid/Schema 拆分 + 叶子窄订阅 + i18n 模块懒加载。

**Architecture:** locale chunk 懒合并；DB 壳+hooks；store 按 tabId equality 订阅。

## Global Constraints

- 中文；**不擅自 git commit**
- `npx tsc -b` 零 error
- 不拆 Toolbox 整文件

---

### Task T0: i18n — [x]

- [x] 脚本按顶层 key 拆 zh-CN / en-US → `i18n/locales/*`
- [x] `ensureLocaleChunks` / boot seed / 模块预热
- [x] `tsc -b`

### Task T1–T4: DB — [x]（首轮）

- [x] `DatabaseDialogsHost` 抽离
- [x] `useTablePreviewForTab` / `useSqlTabStateForTab` / `useTabDirtyRowsForTab`
- [x] `useTableDataGridColumnVisibility`
- [x] `useSchemaBrowserFilters`
- [x] `tsc -b`

说明：Grid/SchemaBrowser 主文件仍大，后续可继续抽 selection / tree 渲染；本轮已建立分文件与窄订阅入口。
